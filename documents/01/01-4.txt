/*
***************************** 01-4 	Фильтры	*********************************65

Рассмотрим таблицу с идентификатором #moto_table
Добавим в таблицу #moto_table еще две строки: Suzuki и Yamaha по цене 25000	
*/
//	Фильтр четных/нечетных элементов. Например, надо выбрать из таблицы только четные строки: 
	        // все строки из таблицы
	 		// только четные строки 0,2 
	  		// только нечетные строки 1,3 

//	Фильтр отрицания. Все, кроме... (Например, нужно перекрасить все ссылки но не зацепить навигацию)
			// Отберем все картинки, кроме мотоциклов 

//	Фильтр по содержанию тэгов. Элемент, который имеет внутри себя тэг...
		    // Отфильтруем только такие формы, которые имеют группировку

//	Фильтр по содержанию текста. Элемент, который имеет внутри себя текст...
			// теги р, которые содержат слово Мотоцикл (чуствительно к регистру)
	    	// первый тег img из данного блока с мотоциклами
	    	// последняя картинка из блока с мотоциклами
	
-----------------------------  З А Д А Н И Е ----------------------------
//	1. Отфильтровать 
	//	четные ячейки из заголовка таблицы, класса mototable
	//	нечетные ячейки из тела всех таблиц (по тегу)
	//	четные (для нас нечетные) элементы списка моделей (список отобрать по идентификатору)
//	2. Фильтр-отрицание
	//	все картинки, кроме хедера (хедер отобрать по вхождению слова logo в атрибут src)
	//	в блоке с id="format" отфильтровать все надписи label, кроме шлема (атрибут for="shlem")
	//	из списка моделей отфильтровать только такие элементы, которые содержат слово "мотоцикл"
//	3. Отфильтровать
	//	такие элементы из всех списков, которые содержат ссылки
	//	такие строки таблицы которые содержат ячейки заголовка
	//	абзацы и заголовки, которые содержат тег span
	
*/