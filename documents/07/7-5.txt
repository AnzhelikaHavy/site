//	7-5 ----- Задание  ---------

	Перед Вами стоит две задачи:		olena.leo.info3@ukr.net		
	
1. 	Добавить проверку e-mail адреса с помощью регулярного выражения (см.ниже). 
	Проверка должна осуществляться в момент ухода из поля для 
	ввода e-mail (событие blur). 
		- Если e-mail соответствует регулярному выражению, то подсветить 
						поле зеленой рамкой, 
		- если  не соответсвует, то красной, 
		- а если в поле находится значение по умолчанию, то серой. 
	При этом надо оставить ту функциональность, при которой в случае 
	пустого ввода возвращается значение по умолчанию.
	
	Рег выражение для проверки e-mail:  [-\w.]+@([A-z0-9][-A-z0-9]+\.)+[A-z]{2,4}
	Метод .search(regV) - зто проверка адреса с помощью регулярного выражения.
	Если такая проверка вернет нам -1, то это значит, что пользователь ввел ерунду.  


// Опишем две переменные: regV и myEmail:	

// Событие ФОКУС...

// Событие УХОД...
	
		// Если ПУСТО - вернуть значение по умолчанию (серая рамка)
        
			// Такого адреса нет (красная рамка)
					
			// Реальный e-mail (зеленая рамка)

   
2. 	Запретить повторную отправку формы., т.е. после однократного 
	нажатия кнопки "Отправить заявку", она должна блокироваться.
	
	Проверка: правильный e-mail: test@test.ru

// Блокировка кнопки отправки формы при повторной попытке.    