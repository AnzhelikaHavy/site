/* К О Р З И Н А - приемник:
	Нужно уложить шлем в корзину при перетаскивании.

1. 	Сначала выбирем интерактивный элемент droppable и скопируем его код (Events:drop)
	Выбираем корзину, применяем метод droppable. 
	-	API documentation 
	- 	Events 
		- drop - drop( event, ui ) 
		- Code examples:	копируем код. 	
	Для события drop: необходимо прописать анонимную функцию      
	drop: function(event, ui)
	Внутрь этой функции нужно прописывать действия на событие drop:
	
2.	Внутрь функции, которая прописывается для падения шлема в корзину:
	-	API documentation - Events - drop(выбранный элемент): $(ui.draggable), 
		так записывается выбранный пользователем текущий элемент.
		Запишем выбранный пользователем шлем в переменную helmet:
		var  helmet = $(ui.draggable);	//Какой шлем нам принесли???
	-	Применим к нашей переменной  метод растворения элемента fadeOut за 0,2сек

3. 	Шлем пока только скрылся. Его нужно теперь переместить из полки в корзину.
	Добавляем после растворения анонимную функцию (записываем function через запятую ):
		Берем текущий элемент $(this), применяем метод appendTo(), а в качестве 
		параметра передадим корзину '#mycart' (вырезать с полки, вставить в корзину).
		Дальше в цепь поставим fadeIn(1000), чтобы показать шлем в корзине.
	(Новый метод: appendTo() - Вырезать элемент и потом вставить)
	(!!! Шлемы пока будут выстраиваться друг под другом в корзине)

4.	Шлемы становятся друг под другом. Вносим изменение в таблице стилей: 
	Нужно поставить левое выравнивание для блоков со шлемами внутри корзины.
	Такое выравнивание уже есть для блоков со шлемами на полке.
	Добавим такое-же выравнивание, тогда получится обтекание справа.
*/
	

