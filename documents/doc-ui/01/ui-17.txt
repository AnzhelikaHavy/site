--------------------- 17.  Прогресс-бар -------------------------

1. Добавляем вконец группы "Опрос предендента" виджет progressbar. 

	- Добавим блок в индексный файл.
	- Перед ним добавим абзац с id="aswerCount": "Дано ответов 0 из 5".
	
---------------------  Обработка progressbar  --------------------
	
1. 	Задаем начальное значение progressbar value: 0

2. 	Выбираем все радио-кнопки из формы внутри блока opros (фильтр :radio).
	На событие change (изменение состояния элемента) накладываем анонимную функцию:

	-	Подсчитаем колличество ответов, тоесть отмеченные радио-кнопки :radio:checked. 
		Метод size вернет кол-во ответов. Запомним в переменную chRadio.
	-	Меняем значение progressbar. Умножаем кол-во ответов на   100/5
	-	Выбираем абзац answerCount и меняем текст чтобы записать туда кол-во ответов:
			'Дано ответов ' + chRadio + ' из ' + '5'
	

-----------------------   Прогресс-бар ------------------------------

1. В индексный файл, в "Опрос предендента" нашей формы: 
	- Добавим  еще два вопроса.
	- Перед ним, исправим абзац с   id="aswerCount": 	"Дано ответов: 0"
	
------------------- Добработка progressbar  -------------------------

1. 	Задаем начальное значение progressbar value: 0

2+.	Выбираем все радио-кнопки из формы внутри блока opros (фильтр :radio).
	На событие change (изменение состояния элемента) накладываем анонимную функцию:
	
		Предусмотрим возможность добавления вопросов в "Опросе претендента".
	+++ Подсчитаем общее кол-во вопросов и положим в переменную questCount:
			-	Можно, например, отобрать все блоки со словом radio внутри атрибута id 
			-	Затем подсчитать кол-во таких блоков применив метод size()
	
	-	Подсчитаем колличество ответов, тоесть отмеченные радио-кнопки :radio:checked. 
		Метод size вернет кол-во ответов. 	Запомним в переменную chRadio.
		
	+++	Меняем значение progressbar. Умножаем кол-во ответов не на 20, а на 100/questCount. 

	+++	Выбираем абзац answerCount и меняем текст чтобы записать туда кол-во ответов:
			'Дано ответов ' + chRadio + ' из ' + 'questCount'

-------------------
3. Уменьшим полоску progressbar до 1em в таблице стилей jquery-ui.css
		.ui-progressbar {height: 1em;...}
	

	
	
	