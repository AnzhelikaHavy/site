****************************  Э Ф Ф Е К Т Ы  *********************************

******************  Еффекты к методам hide() и show() ********************

	Рассмотрим еффекты к методам hide() и show() на примере исчезновения/появления
	формы при щелчке на переключателе 'Свернуть форму' / 'Развернуть форму'
	

****************** 33-1. Метод toggle - переключатель с эффектом ********************	
	
	Применим метод toggle для организации переключателя с определенным эффектом:
		для заголовка 	'Свернуть/Развернуть форму'
	
	// Метод toggle - переключатель с эффектом

*************************   33-2. Метод эффект    ******************************	

Предполодим, что поле дата в нашей форме обязательно должно быть заполнено.
Организуем проверку заполнения этого поля перед отправкой формы и добавим эфекты.
Если дата не введена - добавить эфект пульсация	поля date по нажатию на кнопку:
		
1.	Находим в коде действия, которые происходят при нажатии на кнопку отправки формы 
	(На событие click по кнопке #my_button у нас была прописана функция,
	которая предотвращала стандартное поведение элемента и меняла надпись на кнопке)
	
2.	Внути этой функции с объектом событие добавляем код для еффекта пульсации:
	- Отбираем поле даты и записываем в переменную myDate
	- Прописываем условие: Если поле даты пустое, то 
			- сначала делаем его красным (метод css), 
			- пульсирующим 1 секунду (метод effect - 'pulsate').

3.	Функция возврата:
		После отработки пульсации делаем поле даты снова серым - таким как было.
	
4. Оставляем код который предотвращал стандартное поведение элемента: 
		отправку формы и менял надпись на кнопке отправки формы на 'Оттправляю...'


		
// Проверка заполнения поля дата и блокировка отправки формы







